version: 1
frontend:
  phases:
    build:
      commands:
        - curl -s https://exam-api.jawedsiddiqui.cloud/api/sitemap.xml -o sitemap.xml
  artifacts:
    baseDirectory: .
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
